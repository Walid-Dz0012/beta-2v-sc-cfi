import './globals.css'

export const metadata = {
  title: 'CFI ZOE - منصة التواصل',
  description: 'منصة تواصل اجتماعي آمنة',
}

export default function RootLayout({ children }) {
  return (
    <html lang="ar" dir="rtl">
      <body className="bg-gray-50">
        {children}
      </body>
    </html>
  )
}import './globals.css'

export const metadata = {
  title: 'CFI ZOE - منصة التواصل',
  description: 'منصة تواصل اجتماعي آمنة',
}

export default function RootLayout({ children }) {
  return (
    <html lang="ar" dir="rtl">
      <body className="bg-gray-50">
        {children}
      </body>
    </html>
  )
}
